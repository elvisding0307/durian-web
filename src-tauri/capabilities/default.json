{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "A password management tool.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
